# Core deep-learning stack — pinned to a known-working CUDA-11.8 combination.
# Adjust the torch wheel index if you need a different CUDA / CPU build.
torch>=1.13,<2.4
torchvision>=0.14,<0.19

# Numerical / scientific
numpy>=1.21
scipy>=1.7
scikit-learn>=1.0

# Image / dataset utilities
Pillow>=9.0
pandas>=1.3

# Reproducibility helpers
tqdm>=4.62
